- - - # ✍️ 项目开发核心准则 (claude.md)

      ### 🔄 项目上下文与感知

      - **始终以** `分销系统 Web 管理后台前端开发指南.md` **为核心技术架构参考**，在开始任何新任务前，必须重新阅读它以对齐技术方案和开发规范。
      - **在完成每个任务后请根据 Git 规范给出具体的 Git 提交信息**
      - **开发前务必查阅最新的 API 接口文档 (Swagger)**。前端所有的数据交互都必须严格遵循接口定义，包括请求方法、路径、参数和响应结构。
      - **以** `业务需求文档` **和** `项目概览` **理解业务流程**，明确页面功能背后的"为什么"，确保 UI/UX 设计符合业务流程和角色权限。
      - **严格遵循项目目录结构规范**，保持模块、组件和视图的组织一致性，特别注意 `shadcn-vue` 组件的正确管理方式。
    
      ### ⛩️ 代码结构与技术规范
    
      - **核心技术栈：Vue 3 + Vite + shadcn-vue + Tailwind CSS + TanStack Table + TypeScript**。禁止引入其他UI库或状态管理库，以保持技术统一。
      - **全面拥抱 Composition API 和** `<script setup>` **语法**，这是项目默认的组件编码风格。
      - **代码组织原则**:
    
      - **API 请求**：所有后端接口调用必须在 `/src/api/` 目录下按业务模块封装，并统一使用 `/src/utils/request.ts` 中封装的 Axios 实例。
      - **状态管理**：全局状态必须使用 Pinia 并在 `/src/store/` 目录下分模块管理（如 `user.ts`, `app.ts`）。
      - **路由**：所有页面路由在 `/src/router/` 中集中定义，并实现基于角色的动态路由加载。
      - **UI组件**：`/src/components/ui/` 仅存放 shadcn-vue 组件（由CLI生成），业务组件存放在 `/src/components/business/` 或 `/src/components/shared/`。
    
      - **样式方案**：优先使用 **Tailwind CSS 工具类**。全局样式和主题变量在 `tailwind.config.js` 和 `/src/assets/css/globals.css` 中定义。
    
      ### 🧠 上下文管理与会话连续性策略
    
      为了克服大模型上下文窗口的限制，并确保在长期、多会话的开发中保持一致性，我们采用以下上下文管理策略。**这是所有开发会话的最高指导原则。**
  
      #### 1. 核心状态文件: `project-status.md`
    
      - **目的**: 这是项目的“记忆单元”，一个动态更新的、记录当前项目状态的单一真实来源。
      - **位置**: 项目根目录下的 `project-status.md`。
      - **维护责任**: **开发者（你）** 有责任在每次开发会话结束后，或在完成一个重要里程碑后，立即更新此文件。
    
      #### 2. 会话生命周期 (Session Lifecycle)
    
      所有开发会话都遵循“**读 -> 做 -> 写**”的闭环模式：
    
      - **a. 会话开始 (读状态)**:
    
      - **必须** 首先打开并阅读 `project-status.md`，了解项目的当前进展和接下来的任务。
      - **必须** 使用“会话启动 Prompt”，将 `project-status.md` 中的关键摘要作为上下文提供给 AI。
  
      - **b. 开发过程 (做任务)**:
    
      - 在会话中，遵循“任务驱动”的上下文提供模式，聚焦于当前任务。
  
      - **c. 会话结束 (写状态)**:
    
      - 在结束当前会话前（或完成一个功能后），**必须** 返回并更新 `project-status.md`。
      - **更新内容**:
    
      - 简要总结本次会话完成的工作（代码摘要）。
      - 记录任何重要的技术决策。
      - 更新“已完成模块”列表。
      - 明确“下一步计划”。
    
      ### ✅ Git 与版本控制
  
      - **所有 Git 提交必须严格遵循以下格式**：
        `<type>(<scope>): <description>`
    
      - **示例**: `feat(user-table): 完成用户列表的搜索和分页功能`
      - **示例**: `fix(auth): 修复token刷新后路由权限丢失的问题`
      - **示例**: `style(dashboard): 优化仪表盘卡片布局和响应式设计`
      - `<scope>` 应为具体的功能模块或页面，如 `login`, `layout`, `data-table`, `user-api` 等。
    
      - **分支管理**:

      - `main`: 生产稳定分支。
      - `develop`: 开发主分支。
      - `feature/xxx`: 具体的功能开发分支，如 `feature/user-management`, `feature/data-table-component`。
    
      - **禁止直接向** `main` **或** `develop` **分支提交代码**。所有功能开发都应在 `feature` 分支上进行，并通过 Pull Request (PR) 合并。
  
      ### 🔐 安全与权限
  
      - **Token 处理**：用户登录后，JWT Token 必须存储在 Pinia (`user` store) 和持久化存储中。
      - **请求认证**：封装的 Axios 实例必须在请求拦截器中自动为每个请求添加 `Authorization: Bearer {token}` 头。
      - **路由守卫**：必须在 Vue Router 的全局前置守卫 (`router.beforeEach`) 中实现权限逻辑：
    
      1. 检查是否存在 Token，若无则重定向到登录页。
      2. 若有 Token，则检查是否已获取用户信息和权限。若无，则调用接口获取。
      3. 根据用户角色动态生成并注册可访问的路由。
    
      - **元素级权限**：页面上的按钮、操作等应根据 Pinia 中存储的用户角色/权限列表，使用 `v-if` 或自定义指令进行动态显隐。
    
      ### 🧪 测试与可靠性
    
      - **为核心的、可复用的逻辑（如** `composables` **和** `utils`**）编写单元测试**。推荐使用 Vitest 作为测试框架。
      - **组件测试**：对包含复杂交互逻辑的业务组件（如 `DataTable.vue`、表单组件）进行基本的单元测试，确保其行为符合预期。
      - **API Mock 测试**：在后端接口未就绪时，使用 MSW 或 Mock.js 创建可靠的模拟数据，确保前端功能开发不受阻塞。
  
      ### 🎨 风格与约定
    
      - **编码风格**：严格遵循项目配置的 **ESLint + Prettier** 规范。IDE 应安装相应插件，并开启保存时自动格式化。
      - **命名规范**:
    
      - **组件文件**: `PascalCase.vue` (e.g., `UserTable.vue`, `DataTable.vue`)
      - **变量/函数**: `camelCase` (e.g., `getUserList`, `handleSubmit`)
      - **TS 类型/接口**: `PascalCase` (e.g., `interface UserInfo`, `type ApiResponse`)
      - **常量**: `UPPER_SNAKE_CASE` (e.g., `const API_BASE_URL`)
    
      - **shadcn-vue 使用规范**：
    
      - **绝不手动创建或修改** `/src/components/ui/` **目录下的组件**。始终使用 `npx shadcn-vue@latest add [component]` 命令添加组件。
      - **组件定制**：添加到项目中的 shadcn-vue 组件可以直接修改其源代码以满足项目需求。
      - **业务组件封装**：对于复杂或重复的组件组合（如包含搜索、表格、分页的页面布局），应封装成独立的业务组件，`DataTable.vue` **是最重要的业务组件**。
    
      ### 📚 文档与可读性
    
      - **为所有可复用组件的** `props` **编写清晰的 TypeScript 类型定义和 JSDoc 注释**。
      - **为所有** `composables` **函数和核心工具函数编写详细的 JSDoc 注释**，说明其用途、参数、返回值和使用示例。
      - **复杂的业务逻辑必须添加行内注释**，解释代码的意图（the "why"），而不仅仅是描述代码做了什么（the "what"）。
      - **当新增页面、修改路由或调整核心组件时，及时更新相关文档**。
    
      ### 🧠 AI 行为准则
    
      - **绝不臆测缺失的上下文。** 如果 API 文档不清晰、业务需求有歧义或技术实现不确定，就提问。
      - **绝不捏造不存在的 shadcn-vue 组件、属性或 Tailwind CSS 类名**。开发前先查阅官方文档确认可用性。
      - **严格遵循 shadcn-vue 的使用方式**：只能通过 CLI 添加组件，不能手动创建或复制组件代码。
      - **在生成页面或组件代码时，优先复用** `/src/components/` **中的现有组件**，避免重复造轮子。
      - **遵循已定义的项目结构**。例如，当被要求创建新页面时，应在 `/src/views/` 下创建文件，在 `/src/router/` 中添加路由，并在 `/src/api/` 中定义相关接口。
      - **关注性能优化**：使用 `defineAsyncComponent` 进行路由级别的代码分割，合理使用 `v-memo` 和 `shallowRef` 优化大列表渲染。
    
      ### 🚀 核心开发重点
    
      - **DataTable 组件是整个项目的技术核心**：必须基于 TanStack Table 构建一个高性能、可复用的数据表格组件，支持搜索、排序、分页、筛选和批量操作。
      - **响应式设计**：所有页面和组件都必须在移动端、平板和桌面端正确显示，充分利用 Tailwind CSS 的响应式工具类。
      - **用户体验优先**：合理使用加载状态、错误边界、空状态设计，确保用户操作流畅且有明确反馈。
      - **代码复用性**：识别和抽离可复用的逻辑到 `composables`，避免在多个组件中重复相同的状态管理和业务逻辑。