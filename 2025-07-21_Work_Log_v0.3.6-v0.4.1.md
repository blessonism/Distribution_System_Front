# 项目开发日志 (V0.3.6 - V0.4.1) - 详细改动与核心代码记录

**日期**: 2025-07-21

---

### 1️⃣ 路由系统核心修复 (Commit: `c335690a`)

对路由守卫 `router.beforeEach` 进行了彻底重构，以解决因动态路由加载时机不当而导致的“刷新或直接访问URL时页面404”的顽固问题。

- **● 路由守卫重构 (`src/router/index.ts`)**: 建立了清晰、可靠的导航逻辑。对于已登录用户，在导航前会先检查动态路由是否已加载。如果未加载，它会先获取用户信息和权限，动态添加路由，然后再重新发起导航，确保了流程的原子性和健壮性。

  ```typescript
  // src/router/index.ts
  router.beforeEach(async (to, from, next) => {
    const userStore = useUserStore()
    const token = userStore.token
    // 使用 hasRoute 检查核心动态路由（如Dashboard）是否存在，作为路由是否已加载的判断依据
    const routesLoaded = router.hasRoute('Dashboard')

    if (token) {
      // 如果用户已登录
      if (to.path === '/login') {
        // 如果已登录且目标是登录页，重定向到仪表盘
        next({ path: '/dashboard' })
      } else {
        // 检查动态路由是否已加载
        if (routesLoaded) {
          // 路由已加载，正常放行
          next()
        } else {
          try {
            // 路由未加载，开始获取用户信息、角色和权限
            await userStore.getUserInfo()

            // 根据角色动态生成可访问的路由
            const accessibleRoutes = filterRoutesByRole(asyncRoutes, userStore.roles)
            accessibleRoutes.forEach(route => {
              // 确保不会重复添加
              if (route.name && !router.hasRoute(route.name)) {
                router.addRoute(route)
              }
            })
            
            // 使用 replace: true, 这样导航就不会留下历史记录
            next({ ...to, replace: true })
          } catch (error) {
            // 获取用户信息失败（例如token过期），重置状态并跳转到登录页
            await userStore.$reset()
            next('/login')
          }
        }
      }
    } else {
      // 用户未登录
      if (to.meta.requiresAuth) {
        // 如果目标页面需要认证，重定向到登录页
        next('/login')
      } else {
        // 不需要认证的页面，直接放行
        next()
      }
    }
  })
  ```

---

### 2️⃣ 客资管理模块开发与系统优化 (Commit: `7f07f913`)

从零开始构建了全新的客资管理模块，并对相关的基础工具和布局进行了优化。

- **● API与类型定义 (`src/api/lead.ts`, `src/types/lead.ts`)**: 为客资模块建立了独立的API封装和TypeScript类型定义，提供了清晰的数据结构和请求规范。

  ```typescript
  // src/api/lead.ts
  import { http } from '@/utils/request'
  import type { Lead } from '@/types/lead'

  export function getLeads(params?: any): Promise<{ list: Lead[]; total: number }> {
    return http.get('/leads/all', { params })
  }

  // src/types/lead.ts
  export type LeadStatus = 'PENDING' | 'FOLLOWING' | 'CONVERTED' | 'INVALID'

  export interface Lead {
    id: string
    name: string
    phone: string
    status: LeadStatus
    source: string
    salespersonName: string
    // ...
  }
  ```

- **● 统一HTTP请求工具 (`src/utils/request.ts`)**: 重构了全局`http`请求助手，使其能自动解构后端返回数据（`response.data.data`），极大简化了业务层的API调用代码。

  ```typescript
  // src/utils/request.ts
  export const http = {
    get: async <T = any>(url: string, config?: AxiosRequestConfig): Promise<T> => {
      const response = await request.get(url, config)
      return response.data.data // 核心改动：自动解构两层 data
    },
    post: async <T = any>(url:string, data?: any, config?: AxiosRequestConfig): Promise<T> => {
      const response = await request.post(url, data, config)
      return response.data.data
    },
    // ... put 和 delete 类似
  }
  ```

- **● 解决双滚动条问题 (`src/layouts/MainLayout.vue`)**: 通过调整主布局的CSS，使用绝对定位替代了原有的flex布局，彻底解决了内容超长时页面出现双滚动条的UI缺陷。

  ```html
  <!-- src/layouts/MainLayout.vue -->

  <!-- 旧代码 -->
  <!-- <main class="flex-1 overflow-y-auto bg-gray-50"> -->

  <!-- 新代码 -->
  <main class="absolute top-16 left-0 right-0 bottom-0 overflow-y-auto bg-gray-50">
    <div class="p-4 sm:p-6 lg:p-8">
      <router-view />
    </div>
  </main>
  ```

---

### 3️⃣ 仪表盘优化与修复

对仪表盘的UI显示和数据获取逻辑进行了优化和修复。

- **● 优化面板显示 (Commit: `665a037`)**: 在“待处理事项”卡片中新增了“代理申请审核”项，并调整了图表卡片中标题和按钮的间距，提升了信息密度和视觉效果。

  ```html
  <!-- src/views/dashboard/Index.vue -->
  <div class="flex items-center justify-between">
    <div class="flex items-center">
      <div class="h-10 w-10 rounded-full bg-red-100 flex items-center justify-center text-red-600 mr-3">
        <UserPlusIcon class="h-5 w-5" />
      </div>
      <div>
        <h4 class="font-medium">代理申请审核</h4>
        <p class="text-sm text-gray-500">{{ dashboardData.pending?.agentApplyCount || 3 }} 条待审核</p>
      </div>
    </div>
    <Button variant="outline" size="sm" @click="navigateTo('/agent/list')">查看</Button>
  </div>
  ```

- **● 修复图表数据丢失问题 (Commit: `e9e01dd`)**: 修正了仪表盘的数据请求方式，将其从旧的 `request.get` 切换为新封装的 `http.get`，解决了因请求工具升级导致的数据不兼容问题。

  ```typescript
  // src/views/dashboard/Index.vue

  // 旧代码
  // import request from '@/utils/request'
  // const response = await request.get('/dashboard/stats')
  // dashboardData.value = response

  // 新代码
  import { http } from '@/utils/request'
  const responseData = await http.get('/dashboard/stats')
  dashboardData.value = responseData
  ```

---

### 4️⃣ 组件与渲染方式重构 (Commit: `6b8a745`)

对核心的 `DataTable` 组件和使用它的 `AgentList` 页面进行了技术重构，旨在简化API和提升代码可读性。

- **● `AgentList` 渲染方式重构 (`src/views/agent/AgentList.vue`)**: 大规模地将原先使用 `h()` 渲染函数来构建复杂表格单元格的方式，重构为返回一个包含 `{ template, setup }` 对象的组件化定义。这种方式更接近原生SFC的开发体验，代码更清晰易懂。

  ```typescript
  // src/views/agent/AgentList.vue

  const columns = [
    {
      accessorKey: 'name',
      header: '代理姓名',
      // 旧的 h() 函数渲染方式
      // cell: ({ row }) => {
      //   return h('div', {}, [
      //     h('div', { class: 'font-medium' }, row.name),
      //     h('div', { class: 'text-xs text-gray-500' }, row.phone)
      //   ])
      // }

      // 新的 template/setup 对象渲染方式
      cell: ({ row }) => {
        return {
          template: `
            <div>
              <div class="font-medium">{{ row.name }}</div>
              <div class="text-xs text-gray-500">{{ row.phone }}</div>
            </div>
          `,
          setup() {
            return { row }
          }
        }
      }
    },
    // ...其他列
  ]
  ```
